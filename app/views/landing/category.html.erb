<%= render 'landing/slider' %>

<!-- Category Section -->
<section id="packages-<%= @category.slug %>" class="container-fluid content-section text-center">
  <div class="ro">
    <div class="col-lg-8 col-lg-offset-2">
      <%= render @destinations %>
    </div>
  </div>
</section>