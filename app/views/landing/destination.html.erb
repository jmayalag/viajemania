<!-- About Section -->
<section id="content-<%= @destination.category.slug %>" class="container content-section center-block" style="padding-top: 30px; margin-bottom: 70px;">
  <div class="row">
    <div class="col-lg-5 col-lg-offset-1">
        <a href="<%= landing_category_path(@destination.category) %>" class="destination-link"><i class="fa fa-arrow-left fa-fw"></i> Volver a paquetes</a>
      </div>
  </div>
  <div class="row">
    <div class="col-lg-5 col-lg-offset-1 col-md-4 col-md-offset-2 col-xs-10 col-xs-offset-1">
      <%= image_tag @destination.imagen, class: 'img-responsive pull-left' %>
    </div>
    <div class="col-lg-5 col-xs-12">
      <div class="destination-text text-left text-justify">
        <h3><%= @destination.nombre %></h3>
        <dl>
          <dt>Descripción</dt>
          <dd><%= @destination.descripcion %></dd>
        </dl>
        <p class="destination-cost text-right">Desde <%= @destination.costo %> <%= @destination.moneda %></p>
      </div>
    </div>
    </div>
  </div>
</section>