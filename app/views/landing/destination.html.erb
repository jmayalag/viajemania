<!-- Destination Section -->
<section id="about" class="container content-section text-center">
  <div class="row">
    <div class="col-lg-8 col-lg-offset-2">
      <%= link_to "Volver a paquetes", landing_category_path(@category) %>
      <%= image_tag @destination.imagen %>
      <h2><%= @destination.nombre %></h2>
      <p><%= @destination.descripcion %></p>
    </div>
  </div>
</section>