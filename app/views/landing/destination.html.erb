<!-- About Section -->
<section id="packages-<%= @destination.category.slug %>" class="container content-section center-block">
  <div class="row">
    <div class="col-lg-5 col-lg-offset-1 col-md-4 col-md-offset-2 col-xs-10 col-xs-offset-1">
      <%= image_tag @destination.imagen, class: 'img-responsive pull-left' %>
    </div>
    <div class="col-lg-5 col-xs-12">
      <div class="destination-text text-left text-justify">
        <h3><%= @destination.nombre %></h3>
        <dl>
          <dt>Descripción</dt>
          <dd><%= @destination.descripcion %></dd>
        </dl>
        <p class="destination-cost text-right">Desde <%= @destination.costo %> $</p>
      </div>
    </div>
    </div>
  </div>
</section>